ID: mmt-ui-app-iflow-transport
_schema-version: "2.1"
version: 1.8.4
modules:
  - name: com.sap.cd.maco.mmt.ui.iflow.transport
    type: approuter.nodejs
    path: ./
    parameters:
      disk-quota: 512M
      memory: 256M
      hosts:
        - devops-app-iflow-transport
      stack: cflinuxfs4      
    build-parameters:
      builder: custom
      commands:
        - npm install
        - npm run dev
